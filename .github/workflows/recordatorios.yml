name: Enviar Recordatorios - VERSIÓN CORREGIDA

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

env:
  SUPABASE_URL: https://nllllvztipbrhryzxamm.supabase.co
  SUPABASE_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5sbGxsdnp0aXBicmhyeXp4YW1tIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMxNTkxMjMsImV4cCI6MjA3ODczNTEyM30.mg8mx2ACsG1VDqjt8jw_DyzU6v1aT8rRdpS0ByBGRq4
  SENDGRID_TEMPLATE_ID: d-8c34868a760f46ae987cc04944d697a0

jobs:
  enviar-recordatorios:
    runs-on: ubuntu-latest
    
    steps:
      - name: Configurar zona horaria
        run: |
          echo "🕐 Hora Yucatán: $(TZ='America/Merida' date)"
          echo "📅 Fecha: $(TZ='America/Merida' date +'%Y-%m-%d')"
      
      - name: Instalar dependencias
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl
      
      - name: Verificar estructura de tabla
        run: |
          echo "🔍 Verificando estructura de tabla 'citas'..."
          
          # Verificar tipo de columna hora_recordatorio_enviado
          SQL_QUERY='SELECT column_name, data_type FROM information_schema.columns WHERE table_name = ''citas'' AND column_name IN (''hora_recordatorio_enviado'', ''recordatorio_enviado'')'
          
          curl -s -X POST \
            "${{ env.SUPABASE_URL }}/rest/v1/rpc/execute_sql" \
            -H "apikey: ${{ env.SUPABASE_KEY }}" \
            -H "Authorization: Bearer ${{ env.SUPABASE_KEY }}" \
            -H "Content-Type: application/json" \
            -d "{\"query\": \"$SQL_QUERY\"}" | jq .
      
      - name: Obtener citas para hoy
        id: obtener-citas
        run: |
          FECHA_HOY=$(TZ='America/Merida' date +'%Y-%m-%d')
          HORA_ACTUAL=$(TZ='America/Merida' date +'%H:%M')
          TIMESTAMP_ACTUAL=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
          
          echo "🔍 Buscando citas para hoy: $FECHA_HOY"
          echo "🕐 Hora actual: $HORA_ACTUAL"
          echo "⏰ Timestamp: $TIMESTAMP_ACTUAL"
          
          # Buscar citas NO enviadas
          RESPONSE=$(curl -s -X GET \
            "${{ env.SUPABASE_URL }}/rest/v1/citas?fecha=eq.$FECHA_HOY&estado=in.(aceptada,pendiente,confirmada)&recordatorio_enviado=is.false&select=id,fecha,hora,cliente_id" \
            -H "apikey: ${{ env.SUPABASE_KEY }}" \
            -H "Authorization: Bearer ${{ env.SUPABASE_KEY }}" \
            -H "Content-Type: application/json")
          
          if [ -z "$RESPONSE" ] || [ "$RESPONSE" = "null" ]; then
            RESPONSE="[]"
          fi
          
          CANTIDAD=$(echo "$RESPONSE" | jq '. | length' 2>/dev/null || echo 0)
          echo "📊 Citas encontradas: $CANTIDAD"
          
          echo "$RESPONSE" > /tmp/citas.json
          echo "CITAS_JSON=/tmp/citas.json" >> $GITHUB_OUTPUT
          echo "FECHA_HOY=$FECHA_HOY" >> $GITHUB_OUTPUT
          echo "HORA_ACTUAL=$HORA_ACTUAL" >> $GITHUB_OUTPUT
          echo "TIMESTAMP_ACTUAL=$TIMESTAMP_ACTUAL" >> $GITHUB_OUTPUT
      
      - name: Procesar y enviar recordatorios
        env:
          SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
        run: |
          FECHA_HOY="${{ steps.obtener-citas.outputs.FECHA_HOY }}"
          HORA_ACTUAL="${{ steps.obtener-citas.outputs.HORA_ACTUAL }}"
          TIMESTAMP_ACTUAL="${{ steps.obtener-citas.outputs.TIMESTAMP_ACTUAL }}"
          
          CITAS=$(cat /tmp/citas.json)
          CANTIDAD=$(echo "$CITAS" | jq '. | length')
          
          if [ "$CITAS" = "[]" ] || [ $CANTIDAD -eq 0 ]; then
            echo "✅ No hay citas pendientes para hoy"
            exit 0
          fi
          
          echo "📋 Procesando $CANTIDAD cita(s)..."
          echo ""
          
          EMAILS_ENVIADOS=0
          BD_ACTUALIZADAS=0
          
          for i in $(seq 0 $(($CANTIDAD - 1))); do
            CITA=$(echo "$CITAS" | jq -c ".[$i]")
            
            ID=$(echo "$CITA" | jq -r '.id')
            HORA_CITA=$(echo "$CITA" | jq -r '.hora')
            CLIENTE_ID=$(echo "$CITA" | jq -r '.cliente_id')
            
            echo "========================================"
            echo "📋 CITA ID: $ID - Hora: $HORA_CITA"
            
            # Calcular minutos restantes
            HORA_ACTUAL_MIN=$((10#${HORA_ACTUAL:0:2} * 60 + 10#${HORA_ACTUAL:3:2}))
            HORA_CITA_MIN=$((10#${HORA_CITA:0:2} * 60 + 10#${HORA_CITA:3:2}))
            MINUTOS=$((HORA_CITA_MIN - HORA_ACTUAL_MIN))
            
            echo "⏰ Minutos restantes: $MINUTOS"
            
            # Ventana: 40-120 minutos antes
            if [ $MINUTOS -ge 40 ] && [ $MINUTOS -le 120 ]; then
              echo "🎯 ¡ENVIANDO RECORDATORIO!"
              
              # 1. OBTENER DATOS DEL CLIENTE
              CLIENTE=$(curl -s -X GET \
                "${{ env.SUPABASE_URL }}/rest/v1/clientes?id=eq.$CLIENTE_ID&select=email,nombre" \
                -H "apikey: ${{ env.SUPABASE_KEY }}" \
                -H "Authorization: Bearer ${{ env.SUPABASE_KEY }}" \
                -H "Content-Type: application/json" | jq -r '.[0]')
              
              EMAIL=$(echo "$CLIENTE" | jq -r '.email')
              NOMBRE=$(echo "$CLIENTE" | jq -r '.nombre // "Cliente"')
              
              if [ "$EMAIL" = "null" ] || [ -z "$EMAIL" ]; then
                echo "❌ Email no encontrado"
                continue
              fi
              
              echo "👤 Cliente: $NOMBRE"
              echo "📧 Email: $EMAIL"
              
              # 2. ENVIAR EMAIL CON SENDGRID
              echo "📨 Enviando email..."
              
              EMAIL_JSON='{
                "personalizations": [{
                  "to": [{"email": "'"$EMAIL"'"}],
                  "dynamic_template_data": {
                    "cliente_nombre": "'"$NOMBRE"'",
                    "hora_cita": "'"$HORA_CITA"'",
                    "minutos_restantes": "'"$MINUTOS"'"
                  },
                  "subject": "Recordatorio de cita - Waldos Barber-Shop"
                }],
                "from": {
                  "email": "carlosffcastillo57@gmail.com",
                  "name": "Waldos Barber-Shop"
                },
                "template_id": "'"${{ env.SENDGRID_TEMPLATE_ID }}"'"
              }'
              
              echo "$EMAIL_JSON" > /tmp/email_$ID.json
              
              SEND_CODE=$(curl -s -o /tmp/send_response.txt -w "%{http_code}" \
                -X POST "https://api.sendgrid.com/v3/mail/send" \
                -H "Authorization: Bearer $SENDGRID_API_KEY" \
                -H "Content-Type: application/json" \
                -d @/tmp/email_$ID.json)
              
              if [ "$SEND_CODE" = "202" ]; then
                echo "✅ Email enviado exitosamente"
                EMAILS_ENVIADOS=$((EMAILS_ENVIADOS + 1))
                
                # 3. ACTUALIZAR BD - CON TIMESTAMP CORRECTO
                echo "🔧 Actualizando base de datos..."
                
                # Crear timestamp completo para la BD
                FECHA_COMPLETA="$FECHA_HOY $HORA_ACTUAL:00"
                echo "📅 Fecha completa para BD: $FECHA_COMPLETA"
                
                # Método A: PATCH con timestamp ISO
                PATCH_DATA='{
                  "recordatorio_enviado": true,
                  "hora_recordatorio_enviado": "'"$TIMESTAMP_ACTUAL"'",
                  "actualizado_en": "'"$TIMESTAMP_ACTUAL"'"
                }'
                
                echo "📊 Datos PATCH: $PATCH_DATA"
                
                PATCH_CODE=$(curl -s -o /tmp/patch_response.txt -w "%{http_code}" \
                  -X PATCH "${{ env.SUPABASE_URL }}/rest/v1/citas?id=eq.$ID" \
                  -H "apikey: ${{ env.SUPABASE_KEY }}" \
                  -H "Authorization: Bearer ${{ env.SUPABASE_KEY }}" \
                  -H "Content-Type: application/json" \
                  -d "$PATCH_DATA")
                
                echo "📤 Respuesta PATCH: código $PATCH_CODE"
                
                if [ "$PATCH_CODE" = "200" ] || [ "$PATCH_CODE" = "204" ]; then
                  echo "✅ BD actualizada exitosamente"
                  BD_ACTUALIZADAS=$((BD_ACTUALIZADAS + 1))
                  
                  # VERIFICACIÓN
                  sleep 1
                  echo "🔍 Verificando actualización..."
                  VERIFY=$(curl -s -X GET \
                    "${{ env.SUPABASE_URL }}/rest/v1/citas?id=eq.$ID&select=recordatorio_enviado,hora_recordatorio_enviado,actualizado_en" \
                    -H "apikey: ${{ env.SUPABASE_KEY }}" \
                    -H "Authorization: Bearer ${{ env.SUPABASE_KEY }}" \
                    -H "Content-Type: application/json")
                  
                  echo "📊 Estado actual: $VERIFY"
                  
                else
                  echo "❌ Error en PATCH, intentando método alternativo..."
                  
                  # Método B: SQL directo con timestamp correcto
                  echo "🔄 Intentando con SQL directo..."
                  
                  SQL_UPDATE="UPDATE citas SET recordatorio_enviado = true, hora_recordatorio_enviado = TIMESTAMP '$FECHA_HOY $HORA_ACTUAL:00', actualizado_en = NOW() WHERE id = $ID"
                  
                  SQL_RESPONSE=$(curl -s -o /tmp/sql_response.txt -w "%{http_code}" \
                    -X POST "${{ env.SUPABASE_URL }}/rest/v1/rpc/execute_sql" \
                    -H "apikey: ${{ env.SUPABASE_KEY }}" \
                    -H "Authorization: Bearer ${{ env.SUPABASE_KEY }}" \
                    -H "Content-Type: application/json" \
                    -d "{\"query\": \"$SQL_UPDATE\"}")
                  
                  if [ "$SQL_RESPONSE" = "200" ]; then
                    echo "✅ BD actualizada via SQL"
                    BD_ACTUALIZADAS=$((BD_ACTUALIZADAS + 1))
                  else
                    echo "❌ Error SQL ($SQL_RESPONSE)"
                    echo "📄 Detalles: $(cat /tmp/sql_response.txt 2>/dev/null || echo 'N/A')"
                  fi
                fi
                
              else
                echo "❌ Error SendGrid ($SEND_CODE)"
              fi
              
              # Limpiar
              rm -f /tmp/email_$ID.json /tmp/send_response.txt /tmp/patch_response.txt /tmp/sql_response.txt 2>/dev/null
              
            elif [ $MINUTOS -gt 120 ]; then
              echo "⏳ Demasiado temprano (+2 horas)"
            elif [ $MINUTOS -lt 0 ]; then
              echo "⏳ Cita ya pasó - Marcando como enviada"
              # Marcar como enviada
              curl -s -X PATCH "${{ env.SUPABASE_URL }}/rest/v1/citas?id=eq.$ID" \
                -H "apikey: ${{ env.SUPABASE_KEY }}" \
                -H "Authorization: Bearer ${{ env.SUPABASE_KEY }}" \
                -d '{"recordatorio_enviado": true, "hora_recordatorio_enviado": "'"$TIMESTAMP_ACTUAL"'", "actualizado_en": "'"$TIMESTAMP_ACTUAL"'"}' > /dev/null 2>&1
            else
              echo "❌ Menos de 40 minutos - No enviar"
            fi
            
            echo "========================================"
            echo ""
            sleep 1
          done
          
          echo ""
          echo "📊 RESUMEN FINAL:"
          echo "   ✅ Emails enviados: $EMAILS_ENVIADOS"
          echo "   🗃️  BD actualizadas: $BD_ACTUALIZADAS"
          echo "   🕐 Hora: $HORA_ACTUAL"
          echo "   📅 Fecha: $FECHA_HOY"
      
      - name: Solución manual definitiva
        run: |
          echo ""
          echo "🔧 SOLUCIÓN MANUAL DEFINITIVA"
          echo "=============================="
          echo ""
          echo "EJECUTA en Supabase SQL Editor:"
          echo ""
          echo "-- 1. Verificar estructura de columnas:"
          echo "SELECT column_name, data_type FROM information_schema.columns WHERE table_name = 'citas';"
          echo ""
          echo "-- 2. Opción A: Si hora_recordatorio_enviado es TIMESTAMP:"
          echo "UPDATE citas SET recordatorio_enviado = true, hora_recordatorio_enviado = NOW() WHERE id = 134;"
          echo ""
          echo "-- 3. Opción B: Si quieres cambiar a solo hora:"
          echo "ALTER TABLE citas ALTER COLUMN hora_recordatorio_enviado TYPE TIME USING hora_recordatorio_enviado::time;"
          echo "UPDATE citas SET recordatorio_enviado = true, hora_recordatorio_enviado = '16:00' WHERE id = 134;"
          echo ""
          echo "-- 4. Verificar:"
          echo "SELECT id, hora, recordatorio_enviado, hora_recordatorio_enviado FROM citas WHERE id = 134;"
          echo "=============================="
      
      - name: Finalizar
        run: |
          echo ""
          echo "========================================"
          echo "✅ SISTEMA DE RECORDATORIOS"
          echo "========================================"
          echo "🏁 Finalizado: $(TZ='America/Merida' date)"
          echo "🔧 Corrección: Timestamps ISO 8601"
          echo "🔄 Próxima ejecución: 5 minutos"
          echo "========================================"