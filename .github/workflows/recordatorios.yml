name: 📧 Recordatorios Automáticos

on:
  schedule:
    # Ejecutar cada 5 minutos
    - cron: '*/5 * * * *'
  
  # Permitir ejecución manual desde GitHub
  workflow_dispatch:

env:
  SUPABASE_URL: 'https://nllllvztipbrhryzxamm.supabase.co'

jobs:
  enviar-recordatorios:
    runs-on: ubuntu-latest
    
    steps:
    - name: 🔍 Verificar configuración
      run: |
        echo "🕐 Iniciando envío de recordatorios"
        echo "📅 Fecha: $(date '+%Y-%m-%d %H:%M:%S')"
        echo "📍 Zona horaria: $(date)"
        echo "🔗 Supabase URL: ${{ env.SUPABASE_URL }}"
    
    - name: 📞 Llamar a función de recordatorios
      id: ejecutar-recordatorios
      env:
        SUPABASE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      run: |
        echo "📧 Ejecutando función de recordatorios..."
        echo "🔑 Key disponible: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY != '' }}"
        
        # Verificar que la key está configurada
        if [ -z "$SUPABASE_KEY" ]; then
          echo "❌ ERROR: SUPABASE_SERVICE_ROLE_KEY no está configurada"
          exit 1
        fi
        
        # URL completa de la función
        FUNCTION_URL="${{ env.SUPABASE_URL }}/functions/v1/recordatorios-automaticos"
        echo "🌐 URL de función: $FUNCTION_URL"
        
        # Crear el JSON de datos
        TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
        echo "🕐 Timestamp: $TIMESTAMP"
        
        # Hacer la petición (NOTA: sin backslash al final de las líneas)
        response=$(curl -s -w "\nHTTP_CODE:%{http_code}" \
          -X POST "$FUNCTION_URL" \
          -H "Authorization: Bearer $SUPABASE_KEY" \
          -H "Content-Type: application/json" \
          -H "User-Agent: GitHub-Actions-Recordatorios/1.0" \
          --max-time 180 \
          --retry 2 \
          --retry-delay 5 \
          -d "{\"source\": \"github-actions\", \"timestamp\": \"$TIMESTAMP\"}")
        
        # Extraer código HTTP
        http_code=$(echo "$response" | grep "HTTP_CODE:" | cut -d: -f2)
        body=$(echo "$response" | sed '/HTTP_CODE:/d')
        
        echo "📊 Código HTTP: $http_code"
        echo "📦 Respuesta: $body"
        
        # Guardar salidas correctamente usando delimiter
        echo "http_code<<EOF" >> $GITHUB_OUTPUT
        echo "$http_code" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        
        echo "response_body<<EOF" >> $GITHUB_OUTPUT
        echo "$body" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        
        # Extraer datos de la respuesta para reporte
        if [ ! -z "$body" ]; then
          total_citas=$(echo "$body" | grep -o '"total_citas":[0-9]*' | cut -d: -f2 || echo "0")
          enviados=$(echo "$body" | grep -o '"recordatorios_enviados":[0-9]*' | cut -d: -f2 || echo "0")
          errores=$(echo "$body" | grep -o '"errores":[0-9]*' | cut -d: -f2 || echo "0")
          
          echo "total_citas<<EOF" >> $GITHUB_OUTPUT
          echo "$total_citas" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "enviados<<EOF" >> $GITHUB_OUTPUT
          echo "$enviados" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "errores<<EOF" >> $GITHUB_OUTPUT
          echo "$errores" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
        fi
        
        # Verificar éxito
        if [ "$http_code" = "200" ]; then
          echo "✅ Éxito - Recordatorios procesados"
          exit 0
        else
          echo "❌ Error HTTP: $http_code"
          echo "🔍 Error details: $body"
          exit 1
        fi
    
    - name: 📊 Generar reporte
      if: always()
      run: |
        echo "=========================================="
        echo "     REPORTE DE RECORDATORIOS"
        echo "=========================================="
        echo "🕐 Hora ejecución: $(date '+%H:%M:%S')"
        echo "📅 Fecha: $(date '+%Y-%m-%d')"
        echo "🔧 Workflow: ${{ github.workflow }}"
        echo "🎯 Trigger: ${{ github.event_name }}"
        echo "📤 Status HTTP: ${{ steps.ejecutar-recordatorios.outputs.http_code }}"
        echo "📊 Citas evaluadas: ${{ steps.ejecutar-recordatorios.outputs.total_citas }}"
        echo "✅ Emails enviados: ${{ steps.ejecutar-recordatorios.outputs.enviados }}"
        echo "❌ Errores: ${{ steps.ejecutar-recordatorios.outputs.errores }}"
        echo "⏰ Próxima ejecución: En 5 minutos"
        echo "=========================================="
        
        # Crear archivo de log
        LOG_FILE="recordatorios-${{ github.run_id }}.log"
        echo "# Reporte de Recordatorios" > $LOG_FILE
        echo "Fecha: $(date '+%Y-%m-%d %H:%M:%S')" >> $LOG_FILE
        echo "Workflow: ${{ github.workflow }}" >> $LOG_FILE
        echo "Run ID: ${{ github.run_id }}" >> $LOG_FILE
        echo "Status HTTP: ${{ steps.ejecutar-recordatorios.outputs.http_code }}" >> $LOG_FILE
        echo "Citas evaluadas: ${{ steps.ejecutar-recordatorios.outputs.total_citas }}" >> $LOG_FILE
        echo "Emails enviados: ${{ steps.ejecutar-recordatorios.outputs.enviados }}" >> $LOG_FILE
        echo "Errores: ${{ steps.ejecutar-recordatorios.outputs.errores }}" >> $LOG_FILE
        
        echo "📄 Log guardado en: $LOG_FILE"
    
    - name: 📤 Subir logs como artefacto
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: recordatorios-logs
        path: recordatorios-*.log
        retention-days: 7
    
    - name: 🚨 Notificar en caso de error crítico
      if: failure()
      run: |
        echo "🚨 ATENCIÓN: El proceso de recordatorios falló"
        echo "🔍 Revisar los logs de la Edge Function en Supabase"
        echo "📧 Verificar que la Service Role Key sea correcta"
        echo "🔄 Comprobar que la función 'recordatorios-automaticos' existe y está desplegada"
        echo "📤 Status HTTP recibido: ${{ steps.ejecutar-recordatorios.outputs.http_code }}"
        echo "📦 Respuesta completa: ${{ steps.ejecutar-recordatorios.outputs.response_body }}"