name: Enviar Recordatorios de Citas - Anti Spam

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

env:
  SUPABASE_URL: https://nllllvztipbrhryzxamm.supabase.co
  SUPABASE_ANON_KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im5sbGxsdnp0aXBicmhyeXp4YW1tIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMxNTkxMjMsImV4cCI6MjA3ODczNTEyM30.mg8mx2ACsG1VDqjt8jw_DyzU6v1aT8rRdpS0ByBGRq4
  SENDGRID_TEMPLATE_ID: d-8c34868a760f46ae987cc04944d697a0

jobs:
  enviar-recordatorios:
    runs-on: ubuntu-latest
    
    steps:
      - name: Configurar zona horaria
        run: |
          echo "🕐 Hora Yucatán: $(TZ='America/Merida' date)"
          echo "📅 Fecha: $(TZ='America/Merida' date +'%Y-%m-%d')"
      
      - name: Instalar dependencias
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl
      
      - name: Obtener citas para hoy
        id: obtener-citas
        run: |
          FECHA_HOY=$(TZ='America/Merida' date +'%Y-%m-%d')
          HORA_ACTUAL=$(TZ='America/Merida' date +'%H:%M')
          
          echo "🔍 Buscando citas para: $FECHA_HOY"
          echo "🕐 Hora actual: $HORA_ACTUAL"
          
          RESPONSE=$(curl -s -X GET \
            "${{ env.SUPABASE_URL }}/rest/v1/citas?fecha=eq.$FECHA_HOY&estado=in.(aceptada,pendiente,confirmada)&recordatorio_enviado=eq.false&select=id,fecha,hora,cliente_id,servicio_id,barbero_id" \
            -H "apikey: ${{ env.SUPABASE_ANON_KEY }}" \
            -H "Authorization: Bearer ${{ env.SUPABASE_ANON_KEY }}" \
            -H "Content-Type: application/json")
          
          if [ -z "$RESPONSE" ] || [ "$RESPONSE" = "null" ]; then
            RESPONSE="[]"
          fi
          
          if echo "$RESPONSE" | jq empty 2>/dev/null; then
            CANTIDAD=$(echo "$RESPONSE" | jq '. | length')
            echo "📊 Citas encontradas: $CANTIDAD"
            echo "$RESPONSE" > /tmp/citas.json
            echo "CITAS_JSON=/tmp/citas.json" >> $GITHUB_OUTPUT
          else
            echo "⚠️  JSON inválido, usando array vacío"
            echo "[]" > /tmp/citas.json
            echo "CITAS_JSON=/tmp/citas.json" >> $GITHUB_OUTPUT
            echo "📊 Citas encontradas: 0"
          fi
          
          echo "FECHA_HOY=$FECHA_HOY" >> $GITHUB_OUTPUT
          echo "HORA_ACTUAL=$HORA_ACTUAL" >> $GITHUB_OUTPUT
      
      - name: Procesar y enviar recordatorios
        env:
          SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
        run: |
          FECHA_HOY="${{ steps.obtener-citas.outputs.FECHA_HOY }}"
          HORA_ACTUAL="${{ steps.obtener-citas.outputs.HORA_ACTUAL }}"
          CITAS_JSON_FILE="${{ steps.obtener-citas.outputs.CITAS_JSON }}"
          
          if [ -f "$CITAS_JSON_FILE" ]; then
            CITAS_JSON=$(cat "$CITAS_JSON_FILE")
          else
            CITAS_JSON="[]"
            echo "⚠️  No se encontró archivo de citas, usar array vacío"
          fi
          
          echo "🔍 Recibidas $(echo "$CITAS_JSON" | jq '. | length' 2>/dev/null || echo 0) citas"
          
          if [ "$CITAS_JSON" = "[]" ] || [ -z "$CITAS_JSON" ] || [ "$CITAS_JSON" = "null" ]; then
            echo "ℹ️  No hay citas para procesar hoy ($FECHA_HOY)"
            exit 0
          fi
          
          if ! echo "$CITAS_JSON" | jq empty 2>/dev/null; then
            echo "❌ ERROR: JSON inválido recibido"
            exit 1
          fi
          
          echo "📋 Procesando citas encontradas..."
          
          # Función para crear JSON de SendGrid - CORREGIDA
          crear_json_sendgrid() {
            local EMAIL="$1"
            local NOMBRE="$2"
            local SERVICIO="$3"
            local FECHA="$4"
            local HORA="$5"
            local BARBERO="$6"
            local MINUTOS="$7"
            local DURACION="$8"
            
            # Formatear fecha para español
            DIA_NUM=$(echo "$FECHA" | sed 's/de.*//')
            MES=$(echo "$FECHA" | sed 's/.*de //' | sed 's/ de.*//')
            ANIO=$(echo "$FECHA" | sed 's/.*de //' | sed 's/.* //')
            FECHA_FORMATEADA="$DIA_NUM de $MES de $ANIO"
            
            # Crear asunto completo
            ASUNTO_COMPLETO="Recordatorio de tu cita en Waldos Barber-Shop - $HORA"
            
            jq -n \
              --arg email "$EMAIL" \
              --arg nombre "$NOMBRE" \
              --arg servicio "$SERVICIO" \
              --arg fecha "$FECHA_FORMATEADA" \
              --arg hora "$HORA" \
              --arg barbero "$BARBERO" \
              --argjson minutos "$MINUTOS" \
              --arg duracion "$DURACION" \
              --arg asunto "$ASUNTO_COMPLETO" \
              --arg template_id "${{ env.SENDGRID_TEMPLATE_ID }}" \
            '{
              "personalizations": [{
                "to": [{"email": $email, "name": $nombre}],
                "dynamic_template_data": {
                  "NOMBRE": $nombre,
                  "SERVICIO": $servicio,
                  "FECHA": $fecha,
                  "HORA": $hora,
                  "BARBERO": $barbero,
                  "DURACION": $duracion,
                  "MINUTOS": $minutos,
                  "DIRECCION": "Calle 24-A, Tzucacab, Yucatán",
                  "TELEFONO": "+52 997 151 2023",
                  "GOOGLE_MAPS": "https://maps.google.com/?q=20.063818,-89.0476701"
                },
                "subject": $asunto
              }],
              "from": {
                "email": "carlosffcastillo57@gmail.com",
                "name": "Waldos Barber-Shop"
              },
              "reply_to": {
                "email": "carlosffcastillo57@gmail.com",
                "name": "Waldos Barber-Shop"
              },
              "template_id": $template_id,
              # CONFIGURACIÓN ANTI-SPAM
              "headers": {
                "List-Unsubscribe": "<mailto:carlosffcastillo57@gmail.com?subject=Desuscribirse>",
                "List-Unsubscribe-Post": "List-Unsubscribe=One-Click",
                "Precedence": "bulk"
              },
              "categories": ["appointment-reminders", "transactional"],
              "custom_args": {
                "app_name": "Waldos_Barber_Shop",
                "email_type": "appointment_reminder"
              },
              "mail_settings": {
                "bypass_list_management": {
                  "enable": true
                }
              },
              "tracking_settings": {
                "click_tracking": {
                  "enable": true
                },
                "open_tracking": {
                  "enable": true
                }
              }
            }'
          }
          
          # Contador
          EMAILS_ENVIADOS=0
          EMAILS_ERROR=0
          
          while IFS= read -r CITA; do
            if [ -z "$CITA" ]; then
              continue
            fi
            
            ID=$(echo "$CITA" | jq -r '.id // empty')
            HORA_CITA=$(echo "$CITA" | jq -r '.hora // empty')
            CLIENTE_ID=$(echo "$CITA" | jq -r '.cliente_id // empty')
            SERVICIO_ID=$(echo "$CITA" | jq -r '.servicio_id // empty')
            BARBERO_ID=$(echo "$CITA" | jq -r '.barbero_id // empty')
            
            if [ -z "$ID" ] || [ -z "$HORA_CITA" ] || [ -z "$CLIENTE_ID" ]; then
              continue
            fi
            
            echo ""
            echo "========================================"
            echo "📋 CITA ID: $ID"
            echo "   Hora programada: $HORA_CITA"
            
            # Obtener datos del cliente
            CLIENTE_RESPONSE=$(curl -s -X GET \
              "${{ env.SUPABASE_URL }}/rest/v1/clientes?id=eq.$CLIENTE_ID&select=email,nombre,apellido" \
              -H "apikey: ${{ env.SUPABASE_ANON_KEY }}" \
              -H "Authorization: Bearer ${{ env.SUPABASE_ANON_KEY }}" \
              -H "Content-Type: application/json")
            
            EMAIL=$(echo "$CLIENTE_RESPONSE" | jq -r '.[0].email // empty')
            NOMBRE=$(echo "$CLIENTE_RESPONSE" | jq -r '.[0].nombre // "Cliente"')
            APELLIDO=$(echo "$CLIENTE_RESPONSE" | jq -r '.[0].apellido // ""')
            
            if [ -n "$APELLIDO" ] && [ "$APELLIDO" != "null" ]; then
              NOMBRE_COMPLETO="$NOMBRE $APELLIDO"
            else
              NOMBRE_COMPLETO="$NOMBRE"
            fi
            
            if [ -z "$EMAIL" ] || [ "$EMAIL" = "null" ]; then
              echo "   ❌ Email no encontrado"
              EMAILS_ERROR=$((EMAILS_ERROR + 1))
              continue
            fi
            
            echo "   👤 Cliente: $NOMBRE_COMPLETO"
            echo "   📧 Email: $EMAIL"
            
            # Obtener datos del servicio
            SERVICIO_RESPONSE=$(curl -s -X GET \
              "${{ env.SUPABASE_URL }}/rest/v1/servicios?id=eq.$SERVICIO_ID&select=nombre,duracion" \
              -H "apikey: ${{ env.SUPABASE_ANON_KEY }}" \
              -H "Authorization: Bearer ${{ env.SUPABASE_ANON_KEY }}" \
              -H "Content-Type: application/json")
            
            SERVICIO_NOMBRE=$(echo "$SERVICIO_RESPONSE" | jq -r '.[0].nombre // "Corte de Cabello"')
            DURACION=$(echo "$SERVICIO_RESPONSE" | jq -r '.[0].duracion // "30 minutos"')
            
            # Obtener datos del barbero
            BARBERO_RESPONSE=$(curl -s -X GET \
              "${{ env.SUPABASE_URL }}/rest/v1/barberos?id=eq.$BARBERO_ID&select=nombre" \
              -H "apikey: ${{ env.SUPABASE_ANON_KEY }}" \
              -H "Authorization: Bearer ${{ env.SUPABASE_ANON_KEY }}" \
              -H "Content-Type: application/json")
            
            BARBERO_NOMBRE=$(echo "$BARBERO_RESPONSE" | jq -r '.[0].nombre // "Nuestro Barbero"')
            
            # Calcular minutos restantes
            HORA_ACTUAL_MIN=$((10#${HORA_ACTUAL:0:2} * 60 + 10#${HORA_ACTUAL:3:2}))
            HORA_CITA_MIN=$((10#${HORA_CITA:0:2} * 60 + 10#${HORA_CITA:3:2}))
            MINUTOS=$((HORA_CITA_MIN - HORA_ACTUAL_MIN))
            
            echo "   ⏰ Minutos restantes: $MINUTOS"
            
            # Ventana ampliada: 40-120 minutos antes
            if [ $MINUTOS -ge 40 ] && [ $MINUTOS -le 120 ]; then
              echo "   🎯 ¡ENVIANDO RECORDATORIO! (Ventana: 40-120 min)"
              
              # Formatear fecha bonita
              DIA=$(TZ='America/Merida' date -d "$FECHA_HOY" '+%d')
              MES_NUM=$(TZ='America/Merida' date -d "$FECHA_HOY" '+%m')
              ANIO=$(TZ='America/Merida' date -d "$FECHA_HOY" '+%Y')
              
              case $MES_NUM in
                "01") MES="Enero";; "02") MES="Febrero";; "03") MES="Marzo";;
                "04") MES="Abril";; "05") MES="Mayo";; "06") MES="Junio";;
                "07") MES="Julio";; "08") MES="Agosto";; "09") MES="Septiembre";;
                "10") MES="Octubre";; "11") MES="Noviembre";; "12") MES="Diciembre";;
                *) MES="";;
              esac
              
              FECHA_BONITA="$DIA de $MES de $ANIO"
              
              # Crear JSON y enviar
              JSON_DATA=$(crear_json_sendgrid "$EMAIL" "$NOMBRE_COMPLETO" "$SERVICIO_NOMBRE" "$FECHA_BONITA" "$HORA_CITA" "$BARBERO_NOMBRE" "$MINUTOS" "$DURACION")
              
              # Debug: ver JSON creado
              echo "   📨 JSON creado exitosamente"
              
              echo "$JSON_DATA" > /tmp/sendgrid_$ID.json
              
              echo "   📤 Enviando a SendGrid..."
              
              RESPONSE_CODE=$(curl -s -o /tmp/response_$ID.txt -w "%{http_code}" \
                -X POST "https://api.sendgrid.com/v3/mail/send" \
                -H "Authorization: Bearer $SENDGRID_API_KEY" \
                -H "Content-Type: application/json" \
                -d @/tmp/sendgrid_$ID.json)
              
              if [ "$RESPONSE_CODE" = "202" ]; then
                echo "   ✅ Email enviado exitosamente (Código: $RESPONSE_CODE)"
                EMAILS_ENVIADOS=$((EMAILS_ENVIADOS + 1))
                
                # Marcar como enviado
                echo "   🔧 Actualizando base de datos..."
                PATCH_RESPONSE=$(curl -s -X PATCH \
                  "${{ env.SUPABASE_URL }}/rest/v1/citas?id=eq.$ID" \
                  -H "apikey: ${{ env.SUPABASE_ANON_KEY }}" \
                  -H "Authorization: Bearer ${{ env.SUPABASE_ANON_KEY }}" \
                  -H "Content-Type: application/json" \
                  -H "Prefer: return=minimal" \
                  -d "{\"recordatorio_enviado\": true}")
                
                echo "   ✅ Marcada como enviada en BD"
              else
                echo "   ❌ Error SendGrid (Código: $RESPONSE_CODE)"
                EMAILS_ERROR=$((EMAILS_ERROR + 1))
                if [ -f "/tmp/response_$ID.txt" ]; then
                  echo "   🔍 Detalles del error:"
                  cat /tmp/response_$ID.txt | head -c 300
                fi
              fi
              
              rm -f /tmp/sendgrid_$ID.json /tmp/response_$ID.txt 2>/dev/null
              sleep 2
              
            elif [ $MINUTOS -gt 120 ]; then
              echo "   ⏳ Demasiado temprano (+2 horas)"
            elif [ $MINUTOS -lt 0 ]; then
              echo "   ⏳ La cita ya pasó"
              curl -s -X PATCH "${{ env.SUPABASE_URL }}/rest/v1/citas?id=eq.$ID" \
                -H "apikey: ${{ env.SUPABASE_ANON_KEY }}" \
                -H "Authorization: Bearer ${{ env.SUPABASE_ANON_KEY }}" \
                -d "{\"recordatorio_enviado\": true}" > /dev/null 2>&1
            else
              echo "   ❌ Muy tarde (menos de 40 minutos)"
            fi
            
            echo "========================================"
            
          done < <(echo "$CITAS_JSON" | jq -c '.[]')
          
          echo ""
          echo "📊 RESUMEN DE EJECUCIÓN:"
          echo "   ✅ Emails enviados: $EMAILS_ENVIADOS"
          echo "   ❌ Emails con error: $EMAILS_ERROR"
          echo "   🕐 Hora: $HORA_ACTUAL"
          echo "   📅 Fecha: $FECHA_HOY"
          echo "   🔒 Anti-Spam: Activado"
          echo "   📧 Template: ${{ env.SENDGRID_TEMPLATE_ID }}"
      
      - name: Finalizar
        run: |
          echo ""
          echo "========================================"
          echo "✅ WORKFLOW COMPLETADO - CONFIGURACIÓN ANTI-SPAM"
          echo "========================================"
          echo "🏁 Finalizado: $(TZ='America/Merida' date)"
          echo "📍 Zona horaria: America/Merida (UTC-6)"
          echo "✉️  Servicio: SendGrid con headers anti-spam"
          echo "🏪 Barbería: Waldos Barber-Shop"
          echo "📧 Remitente: carlosffcastillo57@gmail.com"
          echo "⏰ Recordatorios: 40-120 minutos antes"
          echo "🛡️  Configuración: Bypass List Management activado"
          echo "🔄 Próxima ejecución: en 10 minutos"
          echo "========================================"